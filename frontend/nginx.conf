server {
    listen 80;

    # SPA: every route falls back to index.html so React Router can handle it.
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri /index.html;
    }
}
